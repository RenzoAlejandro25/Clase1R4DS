utils::sessionInfo() #esta función me da información de la versión de R, mi tipo de computadora
# y mi sistema operativo. También esta mi campo local (locale). En attached base packages se señala
# los paquetes que se abren cada vez que inicio en R

installed.packages() #este comando me da demasiana información para verla en console

View(installed.packages()) # este comando me permite visulizar los resultados de otro comando en un
# en un formato más amigable, en view la "v" esta en mayúscula

